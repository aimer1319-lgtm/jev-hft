// `npm run dashboard`. The program itself lives in src/dashboard/server.ts; this file exists so
// the dashboard can be started the same way as the other programs (src/<name>.ts), which is
// what the Raspberry Pi service expects.
import './dashboard/server.ts';
